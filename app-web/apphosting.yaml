runConfig:
  minInstances: 0
  maxInstances: 10
  concurrency: 80
  cpu: 1
  memoryMiB: 512

scripts:
  buildCommand: pnpm run build:apphosting
  runCommand: node .apphosting/server.js

outputFiles:
  serverApp:
    include:
      - .apphosting/**
